export type TProduct = {
  name: string;
  price: number;
  sizes: number[];
  uri: string;
};

export type TCategoryProduct = {
  brand: string;
  category: string;
  color: string;
  name: string;
  price: number;
  sizes: number[];
  tag: string;
  uri: string;
};
